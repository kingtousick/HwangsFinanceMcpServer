mcp>=1.2.0
httpx>=0.27
python-dotenv>=1.0
truststore>=0.9  # 사내망 TLS 가로채기 환경: OS 인증서 저장소 사용
# 선택: 최후 수단 렌더링 fallback (설치: pip install playwright && playwright install chromium)
# playwright>=1.44
# 테스트
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
